# ── Core: Voice AI Pipeline ───────────────────────────────────────────────────
# Pinado em >=1.1.0 (versão em produção).
# ATENÇÃO: 1.x tem breaking changes em relação a 0.x:
#   - FrameSerializerType removido de base_serializer
#   - FastAPIWebsocketTransport movido para pipecat.transports.websocket.fastapi
#   - vad_audio_passthrough removido do FastAPIWebsocketParams
#   - LiveOptions agora fornecido pelo pipecat (não pelo deepgram-sdk v6)
pipecat-ai[deepgram,openai,cartesia,silero]>=1.1.0,<2.0.0

# ── Web server ────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
uvloop>=0.20.0

# ── HTTP / WebSocket ──────────────────────────────────────────────────────────
# websockets 14+ removeu o parâmetro `no_delay` do connect(), quebrando o
# deepgram-sdk. Fixar em <14 até que o SDK seja atualizado.
websockets>=13.0,<14.0
httpx>=0.28

# ── Data / Env ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
pydantic>=2.10.6,<3

# ── Storage ───────────────────────────────────────────────────────────────────
redis[hiredis]>=5.0.8

# ── Utilitários ───────────────────────────────────────────────────────────────
tenacity>=8.2.0             # retry com backoff exponencial
serpapi>=0.1.5              # Google Search via SerpApi